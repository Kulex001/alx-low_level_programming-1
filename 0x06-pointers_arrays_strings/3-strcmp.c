#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: string one
 * @s2: string 2
 *
 * Return: their diff
 */
int _strcmp(char *s1, char *s2)
{
	int n;

	n = strcmp(s1, s2);

	return (n);
}
