#include "main.h"
#include <string.h>

/**
 * _strcpy - copy a given string
 * @dest: destination
 * @src: source
 *
 * Return: destination
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
